apply plugin: 'groovy'

group = 'io.github.iboyko.jpamodelgen'
version = '1.0.0-SNAPSHOT'

repositories{ jcenter() }

dependencies {
    compile gradleApi()
    compile localGroovy()

    testCompile ("junit:junit:4.12")
}


wrapper {
    gradleVersion = '2.3'
}